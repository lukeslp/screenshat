NODE_ENV=production
PORT=5091
DATABASE_URL=mysql://screenshat:password@localhost:3306/screenshat
INTERNAL_CAPTURE_KEY=generate-a-random-hex-string

# ── LLM provider ──────────────────────────────────────────────────────────
# Leave unset (or set to "gateway") to use the local api-gateway (default).
# Set to "openai", "anthropic", or "google" for direct provider access.
#
# LLM_PROVIDER=openai
# LLM_API_KEY=sk-...
# LLM_MODEL=gpt-4o            # optional — sensible defaults apply per provider

# Gateway config (only needed when LLM_PROVIDER=gateway or unset)
# API_GATEWAY_URL=http://localhost:5200
# API_GATEWAY_KEY=
